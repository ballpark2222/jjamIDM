name: freedm_application
description: "FreeDM application services (scheduler, queue, policies)."
publish_to: none
version: 0.1.0
environment:
  sdk: ^3.13.0
resolution: workspace
dependencies:
  crypto: ^3.0.7
  freedm_core_domain:
    path: ../core-domain
  freedm_download_api:
    path: ../download-api
  freedm_event_bus:
    path: ../event-bus
  freedm_media_api:
    path: ../media-api
dev_dependencies:
  test: ^1.26.0
  lints: ^6.0.0
  freedm_persistence:
    path: ../persistence
