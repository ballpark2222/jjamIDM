/// FreeDM Brisk adapter — the only package allowed to import
/// brisk_engine. Translates between the vendored engine and the
/// DownloadEngine port (design doc §11).
library;

export 'src/brisk_engine_adapter.dart';
