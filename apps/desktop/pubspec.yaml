name: freedm_desktop
description: "FreeDM desktop app (Flutter)."
publish_to: none
version: 0.1.0
environment:
  sdk: ^3.13.0
resolution: workspace
dependencies:
  flutter:
    sdk: flutter
  freedm_application:
    path: ../../packages/application
  freedm_core_domain:
    path: ../../packages/core-domain
  freedm_download_api:
    path: ../../packages/download-api
  freedm_engine_host:
    path: ../../apps/engine-host
  freedm_event_bus:
    path: ../../packages/event-bus
  freedm_persistence:
    path: ../../packages/persistence
  freedm_update_api:
    path: ../../packages/update-api
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
